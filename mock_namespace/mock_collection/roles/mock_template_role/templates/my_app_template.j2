{% if services %}
services:
  {% for service in services %}
  - name: {{ service.name }}
    port: {{ service.port }}
    enabled: {{ service.enabled | default(false) }}
  {% endfor %}
{% else %}
services: []
{% endif %}

[api]
client_id = {{ client_id }}
client_secret = {{ client_secret }}